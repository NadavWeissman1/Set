<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Set.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Set"
    xmlns:v="clr-namespace:Set.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="Set">
    <ShellContent
        Title="Register"
        ContentTemplate="{DataTemplate v:RegisterPage}"
        Route="RegisterPage" />
    <ShellContent
       Title="Login"
       ContentTemplate="{DataTemplate v:LoginPage}"
       Route="LoginPage" />
    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

</Shell>
